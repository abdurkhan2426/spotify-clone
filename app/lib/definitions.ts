// This file contains type definitions for your data.
// It describes the shape of the data, and what data type each property should accept.

export type currentSongDetails = {
    name: string;
    author: string;
    image_url: string;
};


export type SongField = {
    name: string;
    author: string;
    image_url: string;
};

